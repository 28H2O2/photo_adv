# CLAUDE.md - photo_adv 项目规范

> **第一性原理**：本项目目标是复现并超越 iOS 锁屏照片的“智能裁剪 + 深度效果”体验——让普通用户随手拍的 mediocre 照片，在锁屏上呈现专业、美观、构图精妙的视觉效果。所有决策回到“用户想在锁屏上看到什么好照片”这个本质，而不是追逐技术时髦。

## 项目概述
- **核心问题**：iOS 把“随便一张照片”变成“锁屏壁纸级好看照片”的秘密是什么？我们如何在非 Apple 生态（Web / macOS / 跨平台）做出可落地、效果接近或更好的开源/自用工具？
- **愿景**：做一个**照片智能构图引擎 + 锁屏预览模拟器**，用户拖入照片 → 自动/半自动产出多组“像 iOS 那样”的推荐裁剪 + 主体分离 + 视差建议 → 一键导出适合 iPhone 锁屏的图片（或动态壁纸）。
- **成功标准**：用真实用户“拍得一般”的照片测试，裁剪后主观“好看度”显著提升（主体突出、杂乱边缘去除、构图平衡、与锁屏 UI 元素和谐）。

## 对话与协作规则（严格遵守）
- **始终用简体中文**回复用户。代码、命令、类名、API、文件名、路径保持原文。
- 内部思考、推理必须完全用简体中文。
- 任何任务开始前，如果有关键歧义（目标、输入、输出、平台、精度要求），**必须先用 ask_user_question 澄清**，得到确认后再执行。
- 不要谄媚、不要夸“这是个很好的想法”。直接指出方案问题、风险、更好路径。
- 发现更好的做法直接说。

## 调研规则
- 优先一手来源：Apple 官方文档、WWDC session、Vision / Core ML 头文件、Human Interface Guidelines、实际逆向/工程分析。
- 纳入**近两年（2024-2026）**的相关论文、开源实现、产品实践。
- 所有联网操作**必须通过 web-access skill**（已配置 Edge CDP）或系统允许的 web_search / open_page 工具。需要浏览器交互（登录、动态渲染）时优先 CDP。
- 调研结果必须落盘到 `docs/research/` 或 `docs/references/`，按 `YYYYMMDD-主题.md` 或 `主题-来源.md` 命名，便于后续自进化。
- 不要只看标题和摘要，要读实际实现细节、局限、代码示例。

## 新项目 / 目录与持久化规则（本项目已执行）
- **第一步永远建规范**：本 CLAUDE.md 已创建。任何子目录新增前，先检查是否有更具体的规范。
- 推荐目录结构（已创建基础）：
  ```
  photo_adv/
  ├── CLAUDE.md
  ├── README.md
  ├── docs/
  │   ├── research/          # 调研笔记、论文总结、逆向分析（必）
  │   ├── references/        # 外部参考资料、截图、Apple HIG 摘录
  │   ├── decisions/         # 架构/技术选型 ADR（Architecture Decision Records）
  │   └── results/           # 实验结果、用户照片测试对比、量化/主观评价
  ├── src/                   # 核心实现（按平台/模块拆）
  ├── scripts/               # 工具脚本、数据准备、评测
  ├── tests/                 # 单元 + 视觉回归 + 真实照片集成测试
  └── ...
  ```
- 每次重大变更后，提醒并更新 docs/ 下的自进化文档（仓库逻辑、核心逻辑、问题解决、结果分析）。
- 归档策略：旧实验、被否决方案移到 docs/archive/ （按时间戳）。

## 编码规则
- **超过 30 行的代码文件**，文件开头必须有中文注释块，包含：
  - 功能简述
  - 输入（内容 + 来源路径/格式）
  - 输出（内容 + 保存路径/格式）
  - 如何运行该文件
  - 依赖了哪些关键文件/模块
  - 在整个项目中的作用
- **前端 HTML / TS / Canvas 文件** 同样适用：复杂组件或引擎文件顶部写清楚用途、关键 API、和 iOS 参考点。
- **禁止无关修改**：只改当前任务直接相关的代码。不要“顺手重构”其他模块。
- **不要过度防御性编程**：权衡可读性与鲁棒性。iOS 风格是“简单直接但可靠”。
- **改完必须验证**：
  - 跑测试（test / lint / build / visual check）
  - 不能为了“跑起来”注释掉错误，要找到根本原因修复。
  - 图像处理结果必须人工/脚本目视检查（是否有 artifact、主体被切、构图奇怪）。
- **密钥/模型权重**：绝不提交。使用环境变量、.env、或本地下载脚本。
- **图像质量优先**：裁剪/分割/合成时使用高质量插值（Lanczos 等），避免边缘锯齿、颜色偏移。
- Git 规则：
  - push 前必须 pull。
  - 多个逻辑变更拆成独立 commit。
  - rebase 前检查 unstaged，force-push 前核实。
  - 提交信息清晰，包含范围。

## 交互设计与产品原则（最高优先级）
- **用户体验 > 一切**（技术优雅度其次）。
  - 为目标设计：用户目标是“快速让照片变好看锁屏壁纸”，不是“调一堆参数”。
  - 不要让用户思考：默认给“最佳推荐”（像 iOS 一键美化），高级选项可展开。
  - 系统承担复杂性：自动主体检测 + 多候选构图 + 锁屏 UI 叠加预览（时钟/通知模拟）。
  - 渐进式展示：先给 3-4 个推荐裁剪结果 + 锁屏预览图，一键应用；需要微调再给手动编辑器。
  - 反馈引导行动：裁剪理由要可解释（“主体居中 + 留白平衡 + 避开时钟区”）。
- 锁屏特化约束必须硬编码或可配置：
  - iPhone 屏幕安全区（刘海/动态岛、底部 Home Indicator）
  - 时钟/日期/通知的典型布局位置（可切换不同风格）
  - 视差效果的层分离（前景主体 / 中景 / 背景）
  - 不同机型分辨率与安全边距
- 真实照片测试驱动：收集“拍得一般”的照片（构图松散、主体偏、杂物多）作为 golden set，持续对比改进。

## 技术方向偏好（参考 Apple）
- 优先 **on-device / 隐私优先** 的 ML（Core ML 风格、WebNN、ONNX Runtime Web、MediaPipe、Transformers.js）。
- 主体分离（Subject Matting / Segmentation）：参考 Apple 的 Portrait/Depth 技术栈。
- 显著性 + 构图感知裁剪（Saliency + Aesthetic Composition）。
- 可选轻量生成/增强（但不必须，重点是裁剪与构图）。
- 跨平台原型建议路径：Web 优先（快速验证 UX + 可视化锁屏预览）→ 导出到 iOS / macOS 原生。
- 避免云依赖，除非用户明确选择“更强模型”。

## 测试与验证规则
- 必须包含**真实用户照片测试**（不能只用公开数据集）。
- **前端视觉检查（严格）**：在交付任何 HTML 预览、iPhone mock、canvas 锁屏模拟前，必须人工或用工具检查：
  - 是否出现空白页、元素重叠、文字不可见或裁切
  - 时钟/动态岛/刘海定位是否正确（参考真实 iOS）
  - 主体 lift + depth 分层是否明显（时钟被部分遮挡）
  - 拖拽/缩放是否丝滑、无跳帧
- 图像输出检查：主体是否被错误裁切、构图是否真正“好看”。
- 推荐在 build 后用浏览器打开 dist/index.html 或 `npm run preview` 进行最终视觉验收。
- 自动化 + 人工双重验证。图像 diff 工具 + 主观打分表。

## 何时使用子 Agent / 技能
- 复杂多步实现：考虑 `implement` / `design` skill。
- 设计评审：`grill-with-docs`、`review`。
- 视觉设计/原型：`huashu-design` 或 `frontend-design`。
- 论文/深度分析：对应 perspective skills。
- 诊断 bug：`diagnose`。

## 初始里程碑（待用户确认后细化）
1. 完成 iOS 机制 + 最佳开源替代的深度调研（docs/research/ 产出）。
2. 定义最小可验证产品（MVP）：Web 版上传照片 → 主体分割 + 智能推荐裁剪框 → 锁屏预览（含模拟时钟）→ 导出。
3. 选定技术栈（分割模型 + 裁剪算法 + 渲染）。
4. 实现核心引擎 + 基础 UI。
5. 用 10+ 张真实“一般照片”验证效果，记录对比。
6. 迭代构图规则 / 模型，直到主观接近或超越 iOS。

---

**更新日志**
- 2026-06-03: 项目初始化，创建 CLAUDE.md，建立目录，配置 web-access (Edge)，明确目标为复现 iOS 锁屏智能深度裁剪体验。
- 后续每次重大决策后追加。

严格执行以上规则。违反规则前先修改本文件。
